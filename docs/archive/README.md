# Archive

Superseded design documents, kept rather than deleted so a later reader can see what an approach was
before it changed. Nothing here is authoritative — the current design is in the documents indexed by
[../INDEX.md](../INDEX.md).

Currently empty.
